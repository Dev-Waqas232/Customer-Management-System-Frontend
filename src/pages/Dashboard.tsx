import Layout from "../components/Layout";

export default function DashboardPage() {
  return (
    <Layout>
      <section className="flex items-center justify-center h-screen font-semibold text-2xl font-heading">
        Under development ;)
      </section>
    </Layout>
  );
}
